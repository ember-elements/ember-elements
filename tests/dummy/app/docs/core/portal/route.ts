import Route from '@ember/routing/route';

export default class DocsCorePortal extends Route.extend({
  // anything which *must* be merged to prototype here
}) {
  // normal class body definition here
}
