<svg fill={{getColor}} data-icon={{Icon}} width={{heightWidth}} height={{heightWidth}} viewBox={{viewBox}}>
    <desc id={{titleValue}}>{{titleValue}}</desc>
    {{#each paths as |item index|}}
      <path  d={{item}} key={{index}} fill-rule="evenodd" />
    {{/each}}
        
</svg>