const ns = '[ember-elements]';

export const CLAMP_MIN_MAX = ns + ` clamp: max cannot be less than min`;
export const SPINNER_WARN_CLASSES_SIZE =
  ns + ` <Spinner> Classes.SMALL/LARGE are ignored if size prop is set.`;
